import java.util.Scanner;
public class SubScription {
    Scanner scanner = new Scanner(System.in);

    int number = scanner.nextInt();


    void calculateSubScription(int number) {
        if(number == 1) {

        }
    }
}